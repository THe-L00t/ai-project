# 코인 자동매매 AI 프로젝트 의존성 (Python 3.13 호환)

# 데이터 처리
pandas>=1.5.0,<3.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.9.0,<2.0.0

# 거래소 API
ccxt>=4.0.0
websocket-client>=1.6.0
PyJWT>=2.8.0  # 업비트 JWT 토큰
urllib3>=1.26.0  # HTTP 요청
requests>=2.31.0

# AI/ML 라이브러리 (선택적)
scikit-learn>=1.3.0,<2.0.0
# tensorflow>=2.13.0  # Python 3.13 미지원으로 주석 처리
# pytorch>=2.0.0  # 선택적 사용

# 기술적 지표 (간소화)
# TA-Lib>=0.4.25  # 컴파일 오류 가능성으로 주석 처리
# pandas-ta>=0.3.14b

# 백테스팅 (간소화)
# backtrader>=1.9.78.123  # Python 3.13 미지원
# zipline-reloaded>=2.2.0  # Python 3.13 미지원

# 데이터베이스
SQLAlchemy>=2.0.0
# sqlite3은 Python 내장 모듈이므로 제거
# postgresql-adapter>=2.9.7  # PostgreSQL 사용시

# 환경 설정
python-dotenv>=1.0.0
configparser>=5.3.0

# 로깅 및 모니터링
loguru>=0.7.0
prometheus-client>=0.17.0

# 웹 프레임워크 (대시보드용)
flask>=2.3.0
flask-socketio>=5.3.0
dash>=2.14.0
plotly>=5.15.0
eventlet>=0.33.0

# 뉴스 및 텍스트 분석
feedparser>=6.0.10  # RSS 뉴스 피드
newspaper3k>=0.2.8  # 뉴스 기사 추출
beautifulsoup4>=4.12.0  # 웹 스크래핑
nltk>=3.8.1  # 자연어 처리
textblob>=0.17.1  # 감정 분석
transformers>=4.30.0  # BERT 등 트랜스포머 모델

# 유틸리티
requests>=2.31.0
schedule>=1.2.0
python-telegram-bot>=20.0  # 텔레그램 알림용
retrying>=1.3.4  # 재시도 로직
timeout-decorator>=0.5.0  # 타임아웃 처리
aiohttp>=3.8.0  # 비동기 HTTP 요청
asyncio-throttle>=1.0.2  # 비동기 요청 제한

# 테스팅
pytest>=7.4.0
pytest-cov>=4.1.0

# 데이터 시각화
matplotlib>=3.7.0
seaborn>=0.12.0

# 성능 최적화
numba>=0.57.0
cython>=3.0.0